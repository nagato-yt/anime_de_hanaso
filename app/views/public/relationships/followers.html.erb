<div class="container">
  <h2 class="text-center mt-5">フォローしてくれたユーザー</h2>
  <div class="row justify-content-center">
    <% if @users.exists? %>
      <div class="col-5">
        <%= render 'public/users/user_card', users: @users %>
      </div>
    <% else %>
      <h2 class="text-center mt-5">ユーザーはいません</h2>
    <% end %>
  </div>
</div>
