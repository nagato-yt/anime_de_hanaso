<div class="container">
    <h2 class="text-center mt-5">みんなの投稿</h2>
    <div class="row mt-5">
        <div class="col-lg-3 px-5 px-sm-0 mx-auto">
            <div class="visible-lg sidebar_fixed">
                <!--投稿フォーム-->
                <div class="post-form">
                    <h5 class="text-center">新規投稿</h5>
                    <%= render 'public/posts/post_form', post: @post %>
                </div>
                <!--タグ検索-->
                <div class="tag_search">
                    <div class="col"> 
                        <%= form_tag({controller:"posts",action:"index"}, method: :get) do %>
                            <% Tag.all.each do |t| %>
                                <li><%= check_box :tag_ids, t.name %><%= t.name %></li>
                            <% end %>
                            <%= submit_tag '検索' %>
                        <% end %>
                    </div>
                </div>
            </div>
        </div>
        <!--一覧-->
        <div class="col-sm-12 col-md-8 col-lg-8 px-5 px-sm-0 mx-auto">
            <%= render 'public/posts/post_index', posts: @posts %>
        </div> 
    </div>
    <div class="row justify-content-center">
        <div class="col align-self-start" onselectstart="return false;" onmousedown="return false;">
            <%= image_tag 'keikoku' %>
        </div>
        <div class="col align-self-end" onselectstart="return false;" onmousedown="return false;">
            <%= image_tag 'chuui' %>
        </div>
    </div>
</div>
