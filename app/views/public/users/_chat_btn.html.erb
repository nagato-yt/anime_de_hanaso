<% if current_user != user && current_user.following?(user) && user.following?(current_user) %>
 <%= link_to  public_chat_path(user.id), class: "ml-3" do %>
   <i class="fas fa-envelope"></i> chatを始める
 <% end %>
<% end %>